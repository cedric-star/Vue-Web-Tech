export class RecipeSorter {
    constructor(names, name) {
        this.name = name;
        this.names = names;
        this.sorted = [];
    }

    sort() {
        names.forEach((name) => {

        })
    }
}